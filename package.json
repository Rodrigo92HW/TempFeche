{
  "homepage" : "https://rodrigo92hw.github.io/TempFeche",
  "dependencies": {
    "firebase": "^9.19.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "scripts": {
    "build": "echo 'Nothing to build'",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}